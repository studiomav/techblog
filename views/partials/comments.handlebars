<div class="comments">
  {{#each this}}
  <section class="comment">
    <div class="meta">
      <b>{{user.username}}:</b>
    </div>
    <div class="text">
      {{comment_text}}
    </div>
  </section>
  {{/each}}
</div>